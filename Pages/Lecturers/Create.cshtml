@page
@model Waikato_University.Pages.Lecturers.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Lacturer</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Lacturer.Name" class="control-label"></label>
                <input asp-for="Lacturer.Name" class="form-control" />
                <span asp-validation-for="Lacturer.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Lacturer.Email" class="control-label"></label>
                <input asp-for="Lacturer.Email" class="form-control" />
                <span asp-validation-for="Lacturer.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
